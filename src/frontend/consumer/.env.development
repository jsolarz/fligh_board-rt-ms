# FlightBoard Consumer App Environment Variables - Development
# API Configuration - Leave empty for auto-detection
# REACT_APP_API_URL=http://localhost:5183
# REACT_APP_SIGNALR_URL=http://localhost:5183/flighthub

# Auto-detection will try:
# 1. HTTPS: https://localhost:7022 (if page loaded via HTTPS)
# 2. HTTP: http://localhost:5183 (if page loaded via HTTP)

# For reverse proxy scenarios (API served from same domain)
# REACT_APP_USE_REVERSE_PROXY=true

REACT_APP_ENV=development
REACT_APP_DEBUG=true
